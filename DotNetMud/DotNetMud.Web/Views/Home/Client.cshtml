    <style type="text/css">
        .container {
            background-color: #99CCFF;
            border: thick solid #808080;
            padding: 20px;
            margin: 20px;
        }

        .fromServer {
            font-family: monospace;               
        }

        .fromUser {            
            font-family: monospace;
            font-weight: bold;
            color: purple;
        }

        .fromUser:before {
            content: ">> ";
            color: red; 
        }
    </style>
@{
    ViewBag.Title = "Chat";
}
<h2>Chat</h2>
<div class="container">
    <input type="text" id="message" />
    <input type="button" id="sendmessage" value="Send" />
    <input type="hidden" id="displayname" />
    <ul id="discussion"></ul>
</div>
@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.0.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <script type="text/javascript">
        $(function () {
            //Set the hubs URL for the connection
            // $.connection.hub.url = "http://localhost:8080/signalr";
            // now that we're in a web app, it should happen automatically. 

            // Declare a proxy to reference the hub.
            var chat = $.connection.mudHub;

            // Create a function that the hub can call to broadcast messages.
            chat.client.sendToClient = function (message) {
                // Html encode display name and message.
                var encodedMsg = $('<div class="fromServer" />').text(message);
                // Add the message to the page.
                $('#discussion').append(encodedMsg);
            };

            function sendUserInputToHub() {
                // Call the Send method on the hub.
                var message = $('#message').val(); 
                var encodedMsg = $('<div class="fromUser" />').text(message);
                $('#discussion').append(encodedMsg);
                chat.server.userCommand(message);
                // Clear text box and reset focus for next comment.
                $('#message').val('').focus();
            }

            // Set initial focus to message input box.
            $('#message').focus();
            // Start the connection.
            $.connection.hub.start().done(function () {
                $('#sendmessage').click(sendUserInputToHub);
                $('#message').keyup(function (e) {
                    if (e.keyCode == 13) {
                        sendUserInputToHub();
                    }
                });
            });
        });
    </script>
}